I will build the Admin Dashboard in `src/problem5/frontend` using **React + Vite + TypeScript**, mirroring the **Binance-style dark theme** and architecture from `problem2`.

### **Tech Stack & Dependencies**
- **Core**: React 18, Vite, TypeScript
- **Styling**: Tailwind CSS (with `problem2` color palette), `clsx`, `tailwind-merge`
- **UI Library**: `antd` (for Modals/Notifications, consistent with `problem2`'s latest state) + Custom Tailwind Components
- **Icons**: `lucide-react`
- **HTTP Client**: `axios`
- **Routing**: `react-router-dom`

### **Implementation Steps**
1.  **Project Initialization**
    - Scaffold Vite project in `src/problem5/frontend`.
    - Install dependencies (`tailwindcss`, `antd`, `axios`, `react-router-dom`, `lucide-react`).
    - Configure `tailwind.config.js` with the specific color palette (Primary `#FCD535`, Background `#1E2329`, etc.).

2.  **Core Components Setup**
    - Port **UI Primitives** from `problem2`:
        - `Button` (Variants: Primary/Secondary/Danger)
        - `Input` (Dark themed)
        - `Modal` (Wrapper around Antd Modal with dark theme overrides)
    - Implement **Layout**:
        - `Sidebar` (Navigation)
        - `Header` (Page title)
        - Responsive shell

3.  **Feature Implementation (Resource Admin)**
    - **API Layer**: Create `services/api.ts` to connect to `http://localhost:3000/api/resources`.
    - **Resource List View**:
        - Data table with sorting/filtering.
        - Status badges (Active/Inactive).
        - Action buttons (Edit, Delete).
    - **Create/Edit Resource**:
        - Form using `antd` Form or custom state.
        - Fields: Name (Validation), Description, Status.
    - **Delete Flow**: Confirmation modal before deletion.

4.  **Integration & styling**
    - Apply the "Binance" dark theme globally.
    - Ensure responsive design for mobile/desktop.
